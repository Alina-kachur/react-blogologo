import { StyledBlogList } from "components/BlogList/styles";
import { motion } from "framer-motion";
import styled from "styled-components";

const WrapperFavoritesPage = styled(motion.div)``;

const FavoritesList = styled(StyledBlogList)``;

const EmptyText = styled.h3``;

const DeleteButton = styled.button``;

export { WrapperFavoritesPage, FavoritesList, EmptyText, DeleteButton };
