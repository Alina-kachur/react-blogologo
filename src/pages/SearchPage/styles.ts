import { motion } from "framer-motion";
import styled from "styled-components";

const WrapperSearchPage = styled(motion.div)`
  display: flex;
  flex-direction: column;
  gap: 20px;
`;

export { WrapperSearchPage };
