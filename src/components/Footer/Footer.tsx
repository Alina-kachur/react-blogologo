import { SwitchTheme } from "components/SwitchTheme/SwitchTheme";
import { StyledFooter } from "./styles";

export const Footer = () => {
  return (
    <StyledFooter>
      <SwitchTheme />
    </StyledFooter>
  );
};
