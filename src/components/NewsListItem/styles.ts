import { StyledArticlesListItem } from "components/ArticlesListItem/styles";
import styled from "styled-components";

const StyledNewsListItem = styled(StyledArticlesListItem)``;

export { StyledNewsListItem };
